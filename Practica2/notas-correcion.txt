// metodo load
load -> como save por metodo (construcor recibe stream, delegando en el sceneobject justo como save)

// evitar static cast
cannon = new cannon
object = cannon;


//add object en vez de fire laser para que sea más genérico

// direccion que fuera un vector (con velocidad hacia abajo)
enum ciclico izq abajo derecha abj etc